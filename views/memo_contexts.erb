<html>
  <head>
    <meta charset=utf-8>
    <title>メモ</title>
    <link rel="stylesheet" href="/style.css">
  </head>
    <body>
      <div>
        <%= @memo_info["memos"][params[:id].to_i - 1]["title"] %>
        <%= @memo_info["memos"][params[:id].to_i - 1]["text"] %>
      </div>
      <div class="button">
        <a href="/memo/<%= @memo_info["memos"][params[:id].to_i - 1]["id"] %>/context">
          <button type="button">編集</button>
        </a>
      </div>
    </form>
  </body>
</html>